---
name: add_card_comment
runner_type: "python-script"
description: Add a new comment to a card
enabled: true
entry_point: "add_card_comment.py"
parameters:
  card_id:
    type: string
    description: The ID of the card to add a comment to
    required: true
  comment:
    type: string
    description: The comment to add to the card
    required: true
  api_key:
    type: string
    description: User API Key overriding the system key
  token:
    type: string
    description: User oAuth Token overriding the system key

